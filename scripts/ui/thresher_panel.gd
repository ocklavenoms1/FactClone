extends ProcessorPanel

## Thresher UI (session-building-ui-4 — catch-up from sessions 2-3 oversight).
## Wheat → Grain (E) + Straw (W). Two output slots stacked vertically on
## the right column via ProcessorPanel default layout. No overrides.
##
## Validates ProcessorPanel pattern at 11 consumers post-Session-4.
